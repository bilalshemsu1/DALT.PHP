# PHP foundations lab

This resettable starter belongs to FS05.1. Run it with:

```bash
php issue-summary.php
```

The first issue is rendered and the unsupported status on the second issue is
reported through the exception path.

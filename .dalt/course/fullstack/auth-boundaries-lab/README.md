# Authentication boundaries lab — Batch 8

Small PHP experiments for password storage, server-side sessions, CSRF, and
authorization. Copy `starter/` into `.dalt/workspace/` as each lesson directs.

The scripts use the repository's PHP runtime and DALT framework primitives. They
do not modify the cumulative guided Build application.
